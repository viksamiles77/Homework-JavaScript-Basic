function createAndReturnBigString() {
    const elements = ['Java', 'script', 'is', 'fun', 'to', 'learn'];
    const fullSaying = `${elements[0]} ${elements[1]} ${elements[2]} ${elements[3]} ${elements[4]} ${elements[5]}.`
    console.log(fullSaying);
}

createAndReturnBigString()