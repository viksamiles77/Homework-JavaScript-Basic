function tellStory() { //function
    const story = ['Viktor', 'energized', 'running']; // array
    const fullStory = `This is ${story[0]}. ${story[0]} is a nice person. Today they are ${story[1]}. They are ${story[2]} all day. The end.`; //print story
    return console.log(fullStory); //console log
}

tellStory() //using function